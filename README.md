# BuildMeAPC
A web application that builds custom PC configurations based on user needs, preferences, and budget. Features include compatibility checks, alternative builds, user roles (normal, writer, admin), news articles, shopping lists, and PDF exports.
